// URL base de la API del backend (FastAPI corriendo en local).
// Al desplegar, se cambia por la URL pública del backend en Render.
export const environment = {
  apiUrl: 'http://127.0.0.1:8000/api',
};
